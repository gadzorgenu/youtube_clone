import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

function Library() {
 return (
        <Text style={styles.container}>Library</Text>
    );
}

const styles = StyleSheet.create({
 container: {}
});

export default Library;